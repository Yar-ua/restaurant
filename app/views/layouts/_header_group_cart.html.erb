<!-- партиал ссылки на групповую корзину -->
<li class="nav-item">
  <%= link_to group_cart_path(group_id: @group.id, id: @group_cart.id), 
      class: 'btn btn-success btn-sm', id: 'group_cart' do %>
    	Моя групповая корзина
	<%= my_number_to_currency(@group_cart.total_price) %> 
	(<%=@group_cart.group_line_items.count%>)
  <% end %>
</li>