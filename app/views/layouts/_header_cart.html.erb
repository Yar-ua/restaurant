<% if user_login and @cart %>
  <!-- партиал для ссылки на одиночную корзину -->
  <%= link_to cart_path(id: @cart.id), class: 'btn btn-success btn-sm', id: 'cart' do %>
    Моя корзина
    <%= my_number_to_currency(@cart.total_price) %> (<%=@cart.line_items.count%>)
  <% end %>
<% end %> 

